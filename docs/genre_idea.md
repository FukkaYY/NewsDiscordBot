# ニュースのジャンルに関する機能

## ジャンル付与

- 表示するニュースに自動でジャンルを付ける
- ジャンルは最大3つまで
- ジャンルの一覧は別のドキュメントで管理
- ジャンル表示は専用のフィールドで
- プログラム実行時に読み込んでGeminiに渡す
- 後の拡張（検索や集計）のため、結び付けたジャンルは専用のテーブル `news_genres` に保存する（URLとジャンルの1対多関係）
